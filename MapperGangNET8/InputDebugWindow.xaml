<ui:FluentWindow x:Class="MapperGangNET8.Views.InputDebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="Input Service Debug" Height="600" Width="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Input Service Debug" FontWeight="Bold" FontSize="18" Margin="0,0,0,10"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <ui:ToggleSwitch x:Name="CaptureToggle" Content="Capture Input" Margin="0,0,20,0" Checked="CaptureToggle_Checked" Unchecked="CaptureToggle_Unchecked"/>
                <ui:ToggleSwitch x:Name="BlockToggle" Content="Block Input" Margin="0,0,20,0" Checked="BlockToggle_Checked" Unchecked="BlockToggle_Unchecked"/>
                <ui:ToggleSwitch x:Name="DebugToggle" Content="Debug Mode" Margin="0,0,20,0" Checked="DebugToggle_Checked" Unchecked="DebugToggle_Unchecked"/>
                <ui:Button Content="Clear Log" Margin="0,0,0,0" Click="ClearLog_Click"/>
            </StackPanel>
        </StackPanel>

        <Border Grid.Row="1" BorderBrush="#333333" BorderThickness="1" Margin="0,10">
            <TextBox x:Name="LogTextBox" IsReadOnly="True" 
                     FontFamily="Consolas" FontSize="12"
                     Background="#1E1E1E" Foreground="White"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"/>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Keyboard State" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="KeyboardStateTextBox" IsReadOnly="True" Height="80"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <TextBlock Text="Mouse State" FontWeight="Bold" Margin="0,0,0,5"/>
                <TextBox x:Name="MouseStateTextBox" IsReadOnly="True" Height="80"/>
            </StackPanel>
        </Grid>
    </Grid>
</ui:FluentWindow>